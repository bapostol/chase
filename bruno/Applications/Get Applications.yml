info:
  name: Get Applications
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/v1/applications"
  params:
    - name: archived
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
