info:
  name: Delete Application
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base-url}}/api/v1/applications/{{application-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
