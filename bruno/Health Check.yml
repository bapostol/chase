info:
  name: Health Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{base-url}}/api/v1/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
