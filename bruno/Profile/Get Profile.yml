info:
  name: Get Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/api/v1/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
